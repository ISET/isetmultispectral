function cameraInfo = nkCameraInfo(nefInfo)
% Derive camera state from NEF file header
%
%    cameraInfo = nkCameraInfo(nefInfo);
%
% Example:
%

if ieNotDefined('nefInfo'); error('NEF Info required'); end

return;
